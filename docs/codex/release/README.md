# Release Documents

This directory contains release and packaging procedures.

Use it for:

- desktop release checklists
- versioning policy
- packaging notes
- release safety gates

Implementation specs still belong in `../specs/`; completion reports belong in `../reports/`.

